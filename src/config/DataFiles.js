
export const DataFiles = Object.freeze({
  // Historik administreras via Inställningar och lagras i data/uploads.
  history: Object.freeze([]),

  // Masterdata administreras via Inställningar och lagras i data/uploads.
  articles: null,
  locations: null,
  placements: null,
});
